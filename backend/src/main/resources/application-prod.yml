cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://admin.prolog.jooky.site}

spring:
  config:
    activate:
      on-profile: prod

  data:
    redis:
      host: redis

  datasource:
    url: jdbc:mysql://mysql:3306/prolog?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_general_ci&sessionVariables=character_set_client=utf8mb4,character_set_connection=utf8mb4,character_set_results=utf8mb4&useSSL=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  mandatory-file-encoding: UTF-8

logging:
  level:
    root: info
    com.back: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.security: debug
